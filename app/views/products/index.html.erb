
<%= form_for :product, :url => {:action => "add"} %>
  <%= f.text_area :product %>
  <%= f.submit "Add Product to Cart" %>
<% end %>

<h1>Your Cart</h1>
<ul>
  <% cart.each do |i| %>
    <li><%= i %></li>
  <% end %>
</ul>
