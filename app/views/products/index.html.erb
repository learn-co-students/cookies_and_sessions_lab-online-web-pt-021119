
<%= form_tag(products_add_path, method: "post") do |f| %>
  <input type="text" name="product"/>
  <input type="submit" value="add to cart">
<% end %>

<ul>
  <% session[:cart].each do |product| %>
    <li>
      <%= product %>
    </li>
  <% end %>
</ul>
